{
  "name": "nuxt-blog-scratch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "nuxt generate && push-dir --remote=dist --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "@nuxtjs/prismic": "^1.2.3",
    "nuxt": "^2.14.0"
  },
  "devDependencies": {
    "node-sass": "^4.14.1",
    "push-dir": "^0.4.1",
    "sass-loader": "^9.0.3"
  }
}
